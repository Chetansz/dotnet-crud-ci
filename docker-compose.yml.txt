version: '3.4'

services:
  api:
    build: .
    ports:
      - "5000:80"

  sonarqube:
    image: sonarqube:latest
    ports:
      - "9000:9000"

  zap:
    image: owasp/zap2docker-stable
    ports:
      - "8080:8080"
